# ─── Local Development ───────────────────────────────────────────────────────
# Mirrors .env.production — override values here for local testing only.
# This file is gitignored so secrets never reach the repo.
NEXT_PUBLIC_AI_ENDPOINT=

# Uncomment to test Supabase locally:
# NEXT_PUBLIC_SUPABASE_URL=http://localhost:54321
# NEXT_PUBLIC_SUPABASE_ANON_KEY=<local-anon-key>
